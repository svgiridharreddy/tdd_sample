= form_with(model: salary, local: true) do |form|
  - if salary.errors.any?
    #error_explanation
      %h2
        = pluralize(salary.errors.count, "error")
        prohibited this salary from being saved:
      %ul
        - salary.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit
