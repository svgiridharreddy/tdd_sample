= form_with(model: tweet, local: true) do |form|
  - if tweet.errors.any?
    #error_explanation
      %h2
        = pluralize(tweet.errors.count, "error")
        prohibited this tweet from being saved:
      %ul
        - tweet.errors.full_messages.each do |message|
          %li= message
  .actions
    = form_for tweet do |f|
      = f.text_field :body,placeholder: "what's happening"
      = form.submit "Tweet",class: "tweet-btn",remote: true
